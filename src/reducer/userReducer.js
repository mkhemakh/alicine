const stateInit = {
    Users: [],
}

const userReducer = ( state = stateInit, action = {} ) => {
    switch( action.type )
    {
        default:
            return state;
    }
}

export default userReducer;